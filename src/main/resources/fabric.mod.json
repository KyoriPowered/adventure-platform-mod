{
  "schemaVersion": 1,
  "id": "${project.rootProject.name.toLowerCase()}",
  "version": "${project.version}",
  "name": "${project.rootProject.name}",

  "authors": [
    "zml"
  ],
  "contact": {
    "homepage": "https://github.com/PEXPlugins/text-adapter-fabric",
    "issues": "https://github.com/PEXPlugins/text-adapter-fabric/issues",
    "sources": "https://github.com/PEXPlugins/text-adapter-fabric"
  },

  "license": "MIT",

  "entrypoints": {
    "main": [
      {
        "value":"ca.stellardrift.adventure.fabric.FabricPlatform"
      }
    ]
  },
  "mixins": [
    "adventure-platform-fabric.mixins.json"
  ],
  "custom": {
    "text-version": "${project.ext['versionAdventure']}",
    "modmenu:api": true
  },

  "depends": {
    "fabricloader": ">=0.4.0"
  }
}
